import { Box, Typography } from '@mui/material';

export default function IntroPage() {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h4" component="h1" gutterBottom>
        Full Explainer (Phase 1 Rebuild)
      </Typography>
      <Typography variant="body1">
        Intro content port target is prepared. Mobile directory/search work will be added in this page during Phase 1.
      </Typography>
    </Box>
  );
}
